import { ContactGroupPipe } from './contact-group.pipe';

describe('ContactGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
